syntax = "proto3";

package company.project.domain.v1;

// This import is not used
// TODO Is not reported by the extension nor the buf linter, it is detected if I move it to the other package
import "google/protobuf/timestamp.proto";

message UnusedImport {
  string test = 1;
}
