syntax = "proto3";

package company.project.domain.v1;

message DuplicateMessage {
  string test = 1;
}

// Duplicate message definition
// Solution: Rename the messsage to "NotADuplicateMessageAnymore"
// TODO Breaks lint checks
message DuplicateMessage {
  string another = 1;
}
