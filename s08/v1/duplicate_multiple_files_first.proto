syntax = "proto3";

package company.project.domain.v1;

// This message is duplicated and also present in s08/v1/duplicate_multiple_files_second.proto

// TODO The extension reports the last file saved as the duplicate. So the error disappear and reappear when saving the files in different order.
message DuplicateInMultipleFiles {
  string test = 1;
}
