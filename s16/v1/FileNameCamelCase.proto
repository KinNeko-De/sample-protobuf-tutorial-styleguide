syntax = "proto3";

package s16.v1;

// TODO the extension does not report the wrong type, buf lint does it
// Solution rename the file to file_name_contains_spaces.proto
message BadFileName {
  string test = 1;
}
