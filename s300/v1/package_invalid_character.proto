syntax = "proto3";

// Package name does not contain a slash for each directory
// Solution: Add a dot (.) for each directory: s300.v1
package s300/v1;

message ValidMessage {
  string test_field = 1;
}
