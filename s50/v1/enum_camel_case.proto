syntax = "proto3";

package s50.v1;

// Enum name should be PascalCase
// Solution: rename enum to 'CamelCase'
// enum camelCase {
//   CAMEL_CASE_UNSPECIFIED = 0;
// }
enum camelCase {
  CAMEL_CASE_UNSPECIFIED = 0;
}
